import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "io.ionic.starter",
    appName: "edisons-sleep-app",
    webDir: "dist",
};

export default config;
